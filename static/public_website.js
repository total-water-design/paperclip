(() => { const button = document.querySelector('.nav-toggle'); const nav = document.querySelector('#public-nav'); const theme = document.querySelector('.theme-toggle'); const applyTheme = value => { document.documentElement.dataset.theme = value; localStorage.setItem('twds-public-theme', value); if (theme) { const dark = value === 'dark'; theme.setAttribute('aria-pressed', String(dark)); theme.setAttribute('aria-label', `Switch to ${dark ? 'light' : 'dark'} theme`); theme.textContent = dark ? 'Light theme' : 'Dark theme'; } }; applyTheme(localStorage.getItem('twds-public-theme') || (matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light')); theme?.addEventListener('click', () => applyTheme(document.documentElement.dataset.theme === 'dark' ? 'light' : 'dark')); if (!button || !nav) return; const close = () => { button.setAttribute('aria-expanded', 'false'); nav.classList.remove('is-open'); }; button.addEventListener('click', () => { const open = button.getAttribute('aria-expanded') === 'true'; if (open) close(); else { button.setAttribute('aria-expanded', 'true'); nav.classList.add('is-open'); } }); document.addEventListener('keydown', (event) => { if (event.key === 'Escape' && button.getAttribute('aria-expanded') === 'true') { close(); button.focus(); } }); })();
