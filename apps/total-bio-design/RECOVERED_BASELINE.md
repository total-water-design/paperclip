# Total Bio Design recovered baseline

Source authority: installed Total Bio Design v0.2.3 Go service on the Suite Alpha host.

The files under `Source/web/` were recovered byte-for-byte through the loopback-only service at `127.0.0.1:8081` before UI/UX Contract migration. The biological workbook engine, process-network solver, compliance advisor, model data, and unit catalog are therefore the same customer runtime that was active before this branch correction.

Runtime remains a separate Go service; this recovery does not deploy or modify `/opt/totalbiodesign`.
